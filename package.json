{
  "name": "handwriting_ui",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://rdarbinyan.github.io/handwriting_ui/",
  "dependencies": {
    "@tensorflow/tfjs": "^1.5.2",
    "gh-pages": "^2.2.0",
    "react-scripts": "3.4.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "yarn run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
